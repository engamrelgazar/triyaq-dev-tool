<?php

namespace {Module}\Tables;

use {Module}\Models\{Name};
use Tiryaq\Table\Abstracts\TableAbstract;
use Tiryaq\Table\Actions\DeleteAction;
use Tiryaq\Table\Actions\EditAction;
use Tiryaq\Table\BulkActions\DeleteBulkAction;
use Tiryaq\Table\BulkChanges\CreatedAtBulkChange;
use Tiryaq\Table\BulkChanges\NameBulkChange;
use Tiryaq\Table\BulkChanges\StatusBulkChange;
use Tiryaq\Table\Columns\CreatedAtColumn;
use Tiryaq\Table\Columns\IdColumn;
use Tiryaq\Table\Columns\NameColumn;
use Tiryaq\Table\Columns\StatusColumn;
use Tiryaq\Table\HeaderActions\CreateHeaderAction;
use Illuminate\Database\Eloquent\Builder;

class {Name}Table extends TableAbstract
{
    public function setup(): void
    {
        $this
            ->model({Name}::class)
            ->addHeaderAction(CreateHeaderAction::make()->route('{-name}.create'))
            ->addActions([
                EditAction::make()->route('{-name}.edit'),
                DeleteAction::make()->route('{-name}.destroy'),
            ])
            ->addColumns([
                IdColumn::make(),
                NameColumn::make()->route('{-name}.edit'),
                CreatedAtColumn::make(),
                StatusColumn::make(),
            ])
            ->addBulkActions([
                DeleteBulkAction::make()->permission('{-name}.destroy'),
            ])
            ->addBulkChanges([
                NameBulkChange::make(),
                StatusBulkChange::make(),
                CreatedAtBulkChange::make(),
            ])
            ->queryUsing(function (Builder $query) {
                $query->select([
                    'id',
                    'name',
                    'created_at',
                    'status',
                ]);
            });
    }
}
